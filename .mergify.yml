merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: selfhosted-security-probe
    queue_conditions: []
    merge_conditions:
      - check-success = vm4-never-merge-selfhosted-guard
    checks_timeout: null
