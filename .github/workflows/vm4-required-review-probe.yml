name: VM4 Required Review Probe
on:
  workflow_dispatch:

jobs:
  approved:
    runs-on: ubuntu-latest
    environment: vm4-required-review-probe
    steps:
      - run: echo VM4_REQUIRED_REVIEW_PROBE_OK
